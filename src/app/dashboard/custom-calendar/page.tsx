import { CustomizableGrid } from "@/components/customizable-grid";

export default function CustomCalendar() {
  return <CustomizableGrid initialTopRows={4} initialBottomRows={2} initialCols={6} />;
}
